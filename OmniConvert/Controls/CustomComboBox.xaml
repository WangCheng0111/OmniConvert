<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="OmniConvert.Controls.CustomComboBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Border x:Name="Body"
                Height="32"
                Background="Transparent"
                BorderBrush="#60909090"
                BorderThickness="1"
                CornerRadius="6"
                PointerEntered="Body_PointerEntered"
                PointerExited="Body_PointerExited"
                PointerPressed="Body_PointerPressed"
                PointerReleased="Body_PointerReleased">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="DisplayText"
                           Grid.Column="0"
                           Padding="12,0,0,0"
                           VerticalAlignment="Center"
                           Foreground="#333333"
                           FontFamily="{StaticResource HarmonyOSRegular}"
                           FontSize="13"
                           TextTrimming="CharacterEllipsis" />

                <FontIcon x:Name="Chevron"
                          Grid.Column="1"
                          Glyph="&#xE70D;"
                          FontSize="12"
                          Foreground="#80909090"
                          FontFamily="{ThemeResource SymbolThemeFontFamily}"
                          Margin="0,0,12,0"
                          VerticalAlignment="Center"
                          RenderTransformOrigin="0.5,0.5">
                    <FontIcon.RenderTransform>
                        <RotateTransform x:Name="ChevronTransform" />
                    </FontIcon.RenderTransform>
                </FontIcon>
            </Grid>
        </Border>

        <Popup x:Name="DropDownPopup"
               IsLightDismissEnabled="False"
               ShouldConstrainToRootBounds="True">
            <Border x:Name="PopupBorder"
                    Background="#FFFFFF"
                    BorderBrush="#D0D0D0"
                    BorderThickness="1"
                    CornerRadius="8"
                    Width="{Binding ActualWidth, ElementName=Body}">
                <Border.RenderTransform>
                    <TranslateTransform x:Name="PopupBorderTransform" />
                </Border.RenderTransform>
                <ListView x:Name="ItemList"
                          MaxHeight="240"
                          Padding="4"
                          Background="Transparent"
                          IsItemClickEnabled="True"
                          ScrollViewer.VerticalScrollBarVisibility="Hidden"
                          ItemClick="ItemList_ItemClick">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}"
                                       FontFamily="{StaticResource HarmonyOSRegular}"
                                       FontSize="13"
                                       Foreground="#333333" />
                        </DataTemplate>
                    </ListView.ItemTemplate>
                    <ListView.ItemContainerTransitions>
                        <TransitionCollection />
                    </ListView.ItemContainerTransitions>
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Padding" Value="12,8" />
                            <Setter Property="MinHeight" Value="0" />
                            <Setter Property="Margin" Value="4,2" />
                            <Setter Property="HorizontalContentAlignment" Value="Left" />
                            <Setter Property="UseSystemFocusVisuals" Value="True" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListViewItem">
                                        <Grid>
                                            <VisualStateManager.VisualStateGroups>
                                                <VisualStateGroup x:Name="CommonStates">
                                                    <VisualState x:Name="Normal" />
                                                    <VisualState x:Name="PointerOver">
                                                        <VisualState.Setters>
                                                            <Setter Target="BgBorder.Background" Value="#f5f5f5" />
                                                        </VisualState.Setters>
                                                    </VisualState>
                                                    <VisualState x:Name="Pressed">
                                                        <VisualState.Setters>
                                                            <Setter Target="BgBorder.Background" Value="#eaeaea" />
                                                        </VisualState.Setters>
                                                    </VisualState>
                                                    <VisualState x:Name="Selected">
                                                        <VisualState.Setters>
                                                            <Setter Target="BgBorder.Background" Value="#eeeeee" />
                                                        </VisualState.Setters>
                                                    </VisualState>
                                                    <VisualState x:Name="PointerOverSelected">
                                                        <VisualState.Setters>
                                                            <Setter Target="BgBorder.Background" Value="#e4e4e4" />
                                                        </VisualState.Setters>
                                                    </VisualState>
                                                    <VisualState x:Name="PressedSelected">
                                                        <VisualState.Setters>
                                                            <Setter Target="BgBorder.Background" Value="#d8d8d8" />
                                                        </VisualState.Setters>
                                                    </VisualState>
                                                </VisualStateGroup>
                                            </VisualStateManager.VisualStateGroups>

                                            <Border x:Name="BgBorder"
                                                    CornerRadius="4"
                                                    Background="Transparent" />

                                            <ContentPresenter x:Name="ContentPresenter"
                                                              Content="{TemplateBinding Content}"
                                                              ContentTemplate="{TemplateBinding ContentTemplate}"
                                                              Padding="{TemplateBinding Padding}"
                                                              Foreground="#333333"
                                                              HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                              VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}" />
                                        </Grid>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>
            </Border>
        </Popup>
    </Grid>
</UserControl>
